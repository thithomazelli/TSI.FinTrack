export interface SavingsMovement {
  id: string;
  ownerId: string;
  description: string;
  amount: number;
  date: string;
  typeId: string;
  accountId: string;
  createdAt: string;
  updatedAt: string;
}
